git init (инициализировать репозиторий)
git config --local user.name "Yana" (представляемся, кто писал код?) (--global)
git config --local user.email "Yana" (представляемся, email) (--global)
git status (за чем следит гит? untracked - недобавленные)
git add -A (добавить все)
git commit -a -m"Название коммита" (коммит)
git log (вывести список коммитов)

git clone <ссылка> <название папки> клонировать репозиторий себе на комп с гитхаба